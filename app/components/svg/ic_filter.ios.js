import React from 'react';
import Icon from 'react-native-vector-icons/AntDesign';

const SvgComponent = (props) => <Icon name="filter" size={props.size} color={props.color} />;

export default SvgComponent;
