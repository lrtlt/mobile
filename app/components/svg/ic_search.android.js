import React from 'react';
import Icon from 'react-native-vector-icons/MaterialIcons';

const SvgComponent = (props) => <Icon name="search" size={props.size} color={props.color} />;

export default SvgComponent;
