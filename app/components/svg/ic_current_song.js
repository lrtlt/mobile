import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const SvgComponent = props => <Icon name="musical-notes" size={props.size} color={props.color} />;

export default SvgComponent;
