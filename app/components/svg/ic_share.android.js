import React from 'react';
import Icon from 'react-native-vector-icons/MaterialCommunityIcons';

const SvgComponent = props => <Icon name="share" size={props.size} color={props.color} />;

export default SvgComponent;
