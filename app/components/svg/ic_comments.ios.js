import React from 'react';
import Icon from 'react-native-vector-icons/Ionicons';

const SvgComponent = (props) => <Icon name="md-chatbox-outline" size={props.size - 2} color={props.color} />;

export default SvgComponent;
